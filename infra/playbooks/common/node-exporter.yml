---
- name: Install node exporter
  hosts: all
  connection: ssh
  become: yes
  become_user: root
  become_method: sudo 
  tasks:
  
  - name: Install prometheus
    become: yes
    command: apt install prometheus-node-exporter

